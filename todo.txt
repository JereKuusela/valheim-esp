Drawer:
 - Setting for line widths per type.
 - Some kind of color customization (rgb + string to color).
 * List of priority names (different color and width can be set).

Ship:
 - Forward velocity (and over 30 seconds).
 - Total velocity (and over 30 seconds).
 - Sail power, wind power, wind direction (relative to ship)
 - Global wind direction and strength.
